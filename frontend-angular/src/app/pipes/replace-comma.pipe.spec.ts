import { ReplaceCommaPipe } from './replace-comma.pipe';

describe('ReplaceCommaPipe', () => {
  it('create an instance', () => {
    const pipe = new ReplaceCommaPipe();
    expect(pipe).toBeTruthy();
  });
});
